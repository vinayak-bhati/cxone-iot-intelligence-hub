{
  "name": "aep-stub",
  "version": "1.0.0",
  "description": "MOCK: Minimal stand-in for NICE AEP (Agentic Engagement Plane). Implements: ingest queue, Thread store, Engagement Path evaluator, GET /context, POST /handover. Real implementation: aep-* services in NICE GitHub Enterprise.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "express": "^4.19.2",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
