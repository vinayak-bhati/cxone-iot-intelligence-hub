{
  "name": "cognigy-triage",
  "version": "1.0.0",
  "description": "MOCK: Simulates a Cognigy IoT Triage Flow. Implements the same decision logic as a real Cognigy Flow (warranty check → severity check → auto-resolve vs escalate). Real implementation: Cognigy Flow Studio with HTTP Request nodes calling AEP Context/Handover APIs.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
